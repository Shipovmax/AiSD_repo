'''
Product of Array Except Self
Дан массив nums. Верни массив ans где ans[i] — произведение
всех элементов кроме nums[i]. Без деления, за O(n).

Input:  nums = [1,2,3,4]
Output: [24,12,8,6]

Input:  nums = [-1,1,0,-3,3]
Output: [0,0,9,0,0]

Условие: O(n) время, O(1) доп. память (выходной массив не считается)
'''

def product_except_self(nums: list[int]) -> list[int]:
    pass
