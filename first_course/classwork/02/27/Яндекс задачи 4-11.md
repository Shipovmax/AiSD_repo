4. You now have a decent encryption program, but it can still be improved. The methods `cipher()` and `decipher()` almost completely duplicate each other, which violates the DRY principle: “Don’t Repeat Yourself.”

Improve the code further: instead of two methods, implement a single method named `process_text()`. This method must be universal and work in both directions, for encrypting and decrypting text.

The `process_text()` method must accept three arguments in addition to `self`:

`process_text(self, text, shift, is_encrypt)`

- `text` — the string to encrypt or decrypt;
- `shift` — the number of positions to shift characters during encryption;
- `is_encrypt` — a boolean flag: `True` if the string must be encrypted, `False` if it must be decrypted.

This method must return a string, either encrypted or decrypted depending on the input flag.

After implementation, test your code by calling `process_text()`:

```python
# Your code here.

# Check:
cipher_master = CipherMaster()

print(cipher_master.process_text(
    text="Once a reviewer accepted a project on the first try, and ever since then I have feared them",
    shift=2,
    is_encrypt=True
))

print(cipher_master.process_text(
    text="qpeg c tgxkgygt ceegrvgf c rtqlgev qp vjg hktuv vkog, cpf ukpeg vjgp k jcxg hgctgf jko",
    shift=-3,
    is_encrypt=False
))
```

5. In OneBigWellKnownCompany, for which you are developing a vacation management system, there are full-time and part-time employees.

Both full-time and part-time employees can take paid vacation:

- a full-time employee gets 28 paid vacation days;
- a part-time employee gets 14 paid vacation days.

Full-time employees can also take unpaid vacation.

Write a program that manages employee and vacation data.

What needs to be done:

1. Define the classes `FullTimeEmployee` and `PartTimeEmployee`. They must inherit from `Employee`. Instances of these classes must store employee data: first name, gender, and the number of available vacation days.
2. Add the method `get_unpaid_vacation()` to `FullTimeEmployee`. This method must:
   - accept two parameters: the start date of unpaid vacation (string) and the required number of days (integer);
   - return a message in the following format: `'Unpaid vacation start date: <date>, duration: <number> days.'`
3. In `PartTimeEmployee`, override the `vacation_days` attribute. Its default value must be `14`.

```python
class Employee:
    vacation_days = 28

    def __init__(self, first_name, last_name, gender):
        self.first_name = first_name
        self.last_name = last_name
        self.gender = gender
        self.remaining_vacation_days = Employee.vacation_days

    def consume_vacation(self, days):
        self.remaining_vacation_days -= days

    def get_vacation_details(self):
        return f"Remaining vacation days: {self.remaining_vacation_days}."


# Extend Employee by creating FullTimeEmployee and PartTimeEmployee.

# Usage example:
# full_time_employee = FullTimeEmployee("Robert", "Crusoe", "m")
# print(full_time_employee.get_unpaid_vacation("2023-07-01", 5))
#
# part_time_employee = PartTimeEmployee("Alena", "Pyatnitskaya", "f")
# print(part_time_employee.get_vacation_details())
```

6. Add protected and private attributes and methods to `Employee` and `FullTimeEmployee` by solving the following tasks:

1. Every employee, regardless of role, must have an identifier in the vacation management system. Since the base class defines common attributes, subclasses should inherit them. The identifier does not need to be strictly hidden, but it should be marked as intended for internal use. Add a protected attribute `_employee_id` to `Employee`. Its value must be generated automatically when any instance of the class or its subclass is created. Identifier generation must be implemented in a private method `__generate_employee_id`. The employee identifier must be the result of Python’s built-in `hash()` function applied to the concatenation of first name, last name, and gender.
2. Full-time employees are eligible for vacation pay. It is calculated based on salary, and salary information should not be publicly accessible or accidentally changed. Add a private attribute `__salary` to `FullTimeEmployee`, initialized from the constructor argument, and a private method `__get_vacation_salary` that returns the vacation payment amount equal to 80% of the salary.

Create instances of `FullTimeEmployee` and `PartTimeEmployee`, use their methods, and verify the correct operation of the protected and private elements.

```python
class Employee:
    vacation_days = 28

    def __init__(self, first_name, last_name, gender):
        self.first_name = first_name
        self.last_name = last_name
        self.gender = gender
        self.remaining_vacation_days = Employee.vacation_days

    def consume_vacation(self, days):
        self.remaining_vacation_days -= days

    def get_vacation_details(self):
        return f"Remaining vacation days: {self.remaining_vacation_days}."


class FullTimeEmployee(Employee):
    def get_unpaid_vacation(self, start_date, days):
        return f"Unpaid vacation start date: {start_date}, duration: {days} days."


class PartTimeEmployee(Employee):
    pass


# Usage example:
full_time_employee = FullTimeEmployee("Ivan", "Ivanov", "m", 50000)
print(full_time_employee.get_unpaid_vacation("2023-07-01", 5))

part_time_employee = PartTimeEmployee("Anna", "Petrova", "f")
part_time_employee.consume_vacation(5)
print(part_time_employee.get_vacation_details())
```

7. The “OOP” retail chain needs a product management program.

All products are different and have different attributes: for example, kettlebells in the sports section have a `weight` attribute. Clothing items do not have a `weight` attribute but do have a `size` attribute.

However, every product must have `name` and `quantity_in_stock`.

Your task is to write a program in which you can create product objects of different types, each with its own properties. These objects must have methods that print product data.

The program must define three classes:

1. `Product`, which describes a product in general. Its initializer must accept two parameters: the product name and quantity. Other product classes will inherit from `Product`.
2. In the same class, implement the method `get_info()`, which must return a string in the following format:

`<product name> (in stock: <number>)`

Example:

`Frying pan (in stock: 42)`

3. `Kettlebell` (inherits from `Product`), describing sports kettlebells.

Its constructor must accept, in addition to the parent class parameters, a value for `weight` in kilograms.

Implement the method `get_weight()` in `Kettlebell`. It must output the product name, quantity, and weight:

`<product name> (in stock: <number>). Weight: <number> kg`

Example:

`Kids kettlebell (in stock: 12). Weight: 0.75 kg`

4. `Clothing` (inherits from `Product`), describing clothing items.

Its constructor must accept, in addition to the parent class parameters, a value for `size`.

Implement the method `get_size()` in `Clothing`. It must output the product name, quantity, and size:

`<product name> (in stock: <number>). Size: <size>`

Example:

`Hat with sleeves (in stock: 3). Size: XXL`

To print the part `<product name> (in stock: <number>)`, call the parent method `get_info()` inside `get_weight()` and `get_size()`.

```python
class Product:
    # Implement the class initializer and the get_info() method.
    ...


class Kettlebell(Product):
    # Implement the class initializer and the get_weight() method.
    ...


class Clothing(Product):
    # Implement the class initializer and the get_size() method.
    ...


# To test your code, create a couple of objects
# and call their methods:
small_kettlebell = Kettlebell("Small kettlebell", 15, 2)
shirt = Clothing("T-shirt", 5, "L")

print(small_kettlebell.get_weight())
print(shirt.get_size())
```

8. The OOP retail chain has three types of stores:

- **mini-markets** — open only on weekdays;
- **weekend stores** — open only on Saturdays and Sundays;
- **non-stop markets** — open every day.

Write a program that allows users to determine whether a particular store is open on a given date.

The code defines:

- a parent class `Store`, describing a store in general. In this class:
  - the `address` attribute stores the store address;
  - the `get_info()` method accepts a date string in the `DD.MM.YYYY` format and returns information about whether the store is open on that day;
  - the `is_open()` method accepts a date object; this is a placeholder method that must be overridden in subclasses;
- three child classes: `MiniStore`, `WeekendStore`, and `NonStopStore`.

Your task:

- complete the `get_info()` method in the parent `Store` class so that it returns information about whether the specific store is open on the given day;
- override the `is_open()` method in `MiniStore`, `WeekendStore`, and `NonStopStore` so that it returns `True` when the store is open on the specified date.

For example, for the date `30.03.2024` (Saturday), the `is_open()` method in the subclasses must work as follows:

- `MiniStore.is_open()` must return `False` because mini-markets are closed on weekends.
- `WeekendStore.is_open()` must return `True` because weekend stores are open on Saturdays and Sundays.
- `NonStopStore.is_open()` must return `True` because such stores are always open.

```python
# Import the required library.

class Store:
    def __init__(self, address):
        self.address = address

    def is_open(self, date):
        ...
```
